# Loop notes

One dated line per grow-loop iteration: what shipped, or what blocked it.
- 2026-09-23: shipped the second microsite, `ons-dataset-catalogue` (commit 1e71bbf, feat/stories). It needed a new source, so the `ons-datasets` adapter went into `uk-open-data-connectors` first (PR #6, stacked on the flood PR #2) and was vendored here with `node scripts/sync-connectors.mjs`. The page renders 338 dataset records, 310 stamped 2023 or 2024, 281 flagged as national statistics; those match a fresh call to `api.beta.ons.gov.uk/v1/datasets?limit=1000` today and the production deploy (Vercel READY, 41s). The gauge story was re-checked the same way (2,097 stations, River Thames 55). Environment note: port 3000 is held by another `next-server` on this machine, so the pre-push gate has to run as `E2E_PORT=3100 git push`.
- 2026-09-24: shipped the third microsite, `food-hygiene-registers` (commit 0899071, feat/stories). No vendored source was left unused, so the `food-hygiene-authorities` adapter went into `uk-open-data-connectors` first (PR #16, branch `feat/connectors/add_fsa_food_hygiene_adapter`, 13 unit tests, `npm run check` and the live smoke test both green) and was vendored with `node scripts/sync-connectors.mjs --from /tmp/connectors-fsa`. The page renders the FSA's 363 registers holding 612,721 establishments, Birmingham largest at 10,239; a fresh call returns the same figures and the production deploy is READY. Two audit fixes landed in the same iteration: the gauge copy quoted 2,097 stations while the agency list had moved on, so the copy, the docs, and both gauge snapshots were refreshed to 2,095; and a rebuilt page kept rendering the old figure because Next's `fetch` cache in `.next/cache/fetch-cache` served the previous build's response, so the three build-time fetches now pass `cache: 'no-store'`. Environment notes: the agency station count flickers between 2,094 and 2,095 on calls minutes apart, so the gauge copy carries the build's figure and its data note now says the total moves between builds; and `uk-open-data-connectors` is mid-refactor on PR #9 (`refactor/connectors/make_repo_uk_only`, package scope renamed to `@open-data-connectors`), which will fail the `@nzlab` package-name check in `scripts/sync-connectors.mjs` once it merges, so the next sync needs the script taught the new scope first.
